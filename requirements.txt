# ============================================================
# Meet Companion — Python Dependencies
# ============================================================

# ---- Web Framework ----
fastapi==0.115.12
uvicorn[standard]==0.34.3
python-multipart==0.0.20

# ---- Database ----
# SQLite is the zero-setup default; the Postgres drivers and pgvector are only
# exercised when DATABASE_URL points at Postgres.
sqlalchemy[asyncio]==2.0.41
aiosqlite==0.21.0
asyncpg==0.31.0
psycopg2-binary==2.9.12
pgvector==0.3.6
alembic==1.16.1

# ---- Embeddings & RAG ----
sentence-transformers==4.1.0
langchain-text-splitters==0.3.8
numpy>=1.26.0

# ---- LLM ----
# No vendor SDKs: every provider (OpenAI, Anthropic, Gemini, Ollama, Groq and
# any OpenAI-compatible endpoint) is implemented over httpx in
# app/providers/llm, which keeps the dependency surface small and makes
# adding a provider a code change rather than a dependency change.

# ---- Documents ----
pypdf==5.5.0

# ---- MCP ----
mcp==1.9.3

# ---- HTTP Client ----
httpx==0.28.1

# ---- Configuration ----
pydantic==2.11.4
pydantic-settings==2.9.1
python-dotenv==1.1.0

# ---- Security ----
# passlib removed: unmaintained since 2020 and its bcrypt backend detection
# breaks against bcrypt 4.1+. app/security.py uses bcrypt directly.
bcrypt==4.2.1

# ---- Logging ----
structlog==25.4.0

# ---- Testing ----
pytest==8.4.1
pytest-asyncio==1.0.0
pytest-httpx==0.35.0
