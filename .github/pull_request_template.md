## What this changes

## Why

## How it was tested
- [ ] `python -m pytest`
- [ ] `npm --prefix frontend run lint && npm --prefix frontend run build`
- [ ] Tried it in the UI (if user-facing)

## Notes for reviewers
